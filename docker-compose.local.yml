services:
  postgres:
    image: postgres:18
    environment:
      - POSTGRES_USER=dlcs
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=compositedb
    volumes:
      - postgres_data:/var/lib/postgresql/docker
    ports:
      - "5432:5432"

volumes:
  postgres_data:
